# Redis конфиг для KvartiraBar
# Лимит памяти 64MB для кэша
# Применить на сервере: redis-cli CONFIG SET maxmemory 64mb

# Максимум 64MB для этого проекта
maxmemory 64mb

# При достижении лимита - удаляем редко используемые ключи
maxmemory-policy allkeys-lru
