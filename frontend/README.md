# 🏠 KvartiraBar Frontend

Современное адаптивное веб-приложение для платформы недвижимости KvartiraBar, построенное на React + Vite + Tailwind CSS.

## ✨ Особенности

- 📱 **Полностью адаптивный дизайн** - отлично работает на телефонах, планшетах и компьютерах
- 👥 **Две роли пользователей**:
  - **Покупатель** - просмотр объявлений, избранное, связь с владельцами
  - **Продавец** - создание, редактирование и управление объявлениями
- 🎨 **Современный UI** в стиле realt24.uz
- 🔐 **JWT авторизация** с автоматическим обновлением токенов
- 🗺️ **Интеграция с Яндекс.Картами** для геокодинга
- ⚡ **Быстрая загрузка** благодаря Vite
- 🎭 **Плавные анимации** с Framer Motion

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- Запущенный Django бэкенд на `http://127.0.0.1:8000`

### Установка

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Создайте файл `.env`:**
   ```bash
   # Скопируйте из примера
   cp .env.example .env
   ```

3. **Настройте переменные окружения:**
   ```env
   VITE_API_URL=http://127.0.0.1:8000/api
   ```

4. **Запустите проект:**
   ```bash
   npm run dev
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── Header.jsx      # Навигация с разными меню для ролей
│   ├── PropertyCard.jsx # Карточка недвижимости
│   └── PropertyFilter.jsx # Фильтры поиска
├── contexts/           # React Context
│   └── AuthContext.jsx # Управление авторизацией
├── pages/              # Страницы приложения
│   ├── Login.jsx       # Вход
│   ├── Register.jsx    # Регистрация
│   ├── Home.jsx        # Главная (список объявлений)
│   ├── PropertyDetail.jsx # Детали недвижимости
│   ├── MyProperties.jsx # Мои объявления (продавец)
│   ├── PropertyForm.jsx # Создание/редактирование
│   ├── ContactRequests.jsx # Запросы на контакт (продавец)
│   ├── Favorites.jsx   # Избранное (покупатель)
│   └── Profile.jsx     # Профиль пользователя
├── services/           # API сервисы
│   └── api.js          # Axios конфигурация и методы
├── App.jsx             # Главный компонент с роутингом
├── main.jsx            # Точка входа
└── index.css           # Глобальные стили + Tailwind

```

## 👥 Роли пользователей

### 🏠 Покупатель (Buyer)
**Возможности:**
- ✅ Просмотр всех активных объявлений
- ✅ Поиск и фильтрация по параметрам
- ✅ Просмотр деталей недвижимости
- ✅ Отправка запроса на контакт владельцу
- ✅ Получение контактов владельца после запроса
- ✅ Добавление в избранное
- ✅ Управление избранным

**Страницы:**
- Главная с фильтрами
- Детали объявления с формой контакта
- Избранное

### 💼 Продавец (Seller)
**Возможности:**
- ✅ Все возможности покупателя
- ✅ Создание новых объявлений
- ✅ Редактирование своих объявлений
- ✅ Удаление объявлений
- ✅ Просмотр запросов на контакт
- ✅ Управление статусом запросов
- ✅ Автоматический геокодинг адресов

**Страницы:**
- Мои объявления
- Создание/редактирование объявления
- Запросы на контакт

## 🎨 Компоненты UI

### Header
Адаптивная навигация с разными меню для каждой роли:
- Мобильное гамбургер-меню
- Выпадающее меню профиля
- Быстрые действия (добавить объявление для продавцов)

### PropertyCard
Красивая карточка недвижимости:
- Изображение с badges (тип сделки, статус)
- Цена, адрес, характеристики
- Кнопки действий в зависимости от роли
- Избранное для покупателей
- Редактирование/удаление для продавцов

### PropertyFilter
Мощный фильтр поиска:
- Текстовый поиск
- Фильтры: тип сделки, комнаты, цена
- Сворачиваемые расширенные фильтры

## 🔐 Авторизация

### JWT Токены
- Access Token (60 минут)
- Refresh Token (1 день)
- Автоматическое обновление при истечении
- Сохранение в localStorage

### Защищенные маршруты
```jsx
<ProtectedRoute>          // Требует авторизации
<ProtectedRoute sellerOnly>  // Только для продавцов
<ProtectedRoute buyerOnly>   // Только для покупателей
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик (быстрее Webpack)
- **React Router v6** - Маршрутизация
- **Axios** - HTTP клиент
- **Tailwind CSS** - Utility-first CSS
- **React Icons** - Иконки
- **React Toastify** - Уведомления
- **Framer Motion** - Анимации
- **React Loading Skeleton** - Скелетоны загрузки

## 📱 Адаптивность

Приложение полностью адаптивно на всех устройствах:

- **Desktop** (1280px+): 3 колонки карточек, полное меню
- **Tablet** (768px-1279px): 2 колонки, компактное меню
- **Mobile** (<768px): 1 колонка, гамбургер-меню

## 🎯 Ключевые функции

### Для покупателей
1. **Поиск недвижимости** с фильтрами
2. **Связь с владельцем**:
   - Отправка сообщения
   - Получение контактов (телефон, email)
3. **Избранное** для сохранения интересных объявлений

### Для продавцов
1. **Управление объявлениями**:
   - Создание с автоматическим геокодингом
   - Редактирование
   - Удаление
2. **Запросы на контакт**:
   - Просмотр заинтересованных покупателей
   - Управление статусом (связались, завершено)
   - Доступ к контактам покупателей

## 🚀 Команды

```bash
npm run dev      # Запуск dev сервера (localhost:3000)
npm run build    # Production сборка
npm run preview  # Просмотр production сборки
```

## 🔧 Конфигурация

### Vite Proxy
Настроен прокси для API запросов в `vite.config.js`:
```javascript
proxy: {
  '/api': {
    target: 'http://127.0.0.1:8000',
    changeOrigin: true,
  }
}
```

### Tailwind
Кастомная конфигурация в `tailwind.config.js`:
- Цветовая палитра primary/secondary
- Кастомные компоненты (btn, card, badge)
- Адаптивные breakpoints

## 📝 Примеры использования

### Регистрация продавца
```javascript
// Выбрать роль "Продавец" при регистрации
// После входа будут доступны:
// - Кнопка "Добавить объявление"
// - Страница "Мои объявления"
// - Страница "Запросы на контакт"
```

### Связь с владельцем
```javascript
// Покупатель открывает объявление
// Вводит сообщение в форму
// Отправляет запрос
// Получает контакты: телефон и email владельца
```

## 🐛 Решение проблем

**Проблема:** CORS ошибки
```bash
# Убедитесь, что Django настроен:
# - CORS_ALLOWED_ORIGINS содержит http://localhost:3000
# - corsheaders в MIDDLEWARE
```

**Проблема:** 401 Unauthorized
```bash
# Очистите localStorage и перелогиньтесь:
localStorage.clear()
```

**Проблема:** Стили не загружаются
```bash
# Переустановите зависимости:
rm -rf node_modules
npm install
```

## 📄 Лицензия

© 2025 KvartiraBar. Все права защищены.

## 🤝 Поддержка

При возникновении вопросов или проблем:
1. Проверьте, что бэкенд запущен
2. Проверьте консоль браузера на ошибки
3. Проверьте network tab для API запросов

---

**Разработано с ❤️ для KvartiraBar**
